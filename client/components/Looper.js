import React from 'react'
import * as Tone from 'tone'

export default function LoopStation() {
  // Tone.Transport.loop = true
  // Tone.Transport.loopEnd = '1m'

  return <h1 style={{color: '#FE1BCB'}}>Loop Station</h1>
}
